module.exports = { config: { name: 'ping', description: 'Check bot latency', usage: 'ping', category: 'utility', permissions: [], cooldown: 2 }, run: async (args, message, event, api) => { try { const start = Date.now(); const msg = await api.sendMessage('🏓 Pong!', message.threadID); const latency = Date.now() - start; await api.editMessage(`🏓 Pong! Latency: ${latency}ms`, msg.messageID); } catch (error) { console.error('Ping command error:', error); await api.sendMessage(`❌ Error: ${error.message}`, message.threadID); } } };